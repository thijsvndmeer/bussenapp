Make a commit with every feature implemented.

# Ads when the player exits the game (When clicking the X going to spel stoppen, play interstitial ad)

# Ads when switching card styles (When switching from one card style to another, play rewarded ad, clearly UI add video buttons to the card styles, making it visible players have to watch an ad to switch, only switch styles after the ad has played)

# Only subtract rotated/played cards from bus pakje (right now all cards are subtracted from the bus pakje, even if they are not played/rotated)

# In the pyramid phase, make the current 'slok indicators' like 1x, 2x, 3x 4x, 5x etc more visible, and dont make them collide with the cards. use beer emojis. like this: 🍺 🍺🍺 🍺🍺🍺 🍺🍺🍺🍺 🍺🍺🍺🍺🍺 but keep it minimal

# Make the effect that plays with 'deze kaart kan nog niet' way more visible. the red is not really visible, keep the outline approach, but use more visible colors and improve the effect and animation.

# In the bus phase, make the sure X to quit button and passagier text is in the top right corner. Generally make the X to quit button always be in the same spot, its also too high in the pyramid phase.

# Make the disco sound stop playing if the player continues with the game (when they click 'ga verder')

# Improve the gedeelde bus UI. the player character should no longer bounce, and when a player is selected, their profile picture should animate with a fun animation, next to the profile picture write who it is 'speler 1', and then it should say 'samen in de bus' and as subtext '{underlined_player1_name} & {underlined_player2_name}' gaan samen in de bus.

# Make sure the translation to English is full. i noticed there are some bits in dutch left.

# Improve the look of the 'to the end' button. Quitting the game shouldnt be possible when this button has appeared. So the X should then disappear

# When a player has a shield and is immune, it should show the text '{name} is immune to the bus, they went in the bus last round'. this should be above the name input field, below the 'players' field. the players field is flexible so it would be a bit shorter when the text is there.

# Add a space between 'RoundN / M' during the game.

# Make the switch app bar not be visible while playing the game, same goes for the status bar.

# Add this all to the patch notes, in order of importance, and make the code ready for the next release.
